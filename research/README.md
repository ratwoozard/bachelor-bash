# Research / rådata

Rå udtræk, CSV og SQL-resultater. Rapporten citerer **herfra** eller fra bilag — aldrig fra hukommelse.

`research/data/*.json` og `*.csv` er gitignored. Læg et kort `README.md` i `data/`, der beskriver *hvad* filen indeholder, uden at committe selve dumpet, hvis data er fortrolige.

Ingen opfundne tal. Tom mappe = ingen kvantitative fund.
